# All of these are ALREADY installed on the Jetson (verified 2026-07-22).
# Listed only for reference / non-Jetson machines. Do NOT pip install on the Jetson
# unless something is missing — versions there are pinned to what works on JetPack.
torch==2.4.1
torchvision==0.19.1
numpy==1.24.4
opencv-python==4.5.4.60
pillow==7.0.0
matplotlib==3.1.2
scikit-learn==1.3.2
scipy==1.10.1
